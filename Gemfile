source 'http://ruby.taobao.org/'

gem 'rails', '5.0.0'
gem "mongoid", "~> 2.4"
gem "bson_ext", "~> 1.5"


# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'



# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails', '~> 5.0.5'
  gem 'coffee-rails', '~> 4.1.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platform => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails', '>= 4.4.0'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'

gem 'steak', '>= 2.0.0', :group => [:test, :development]
gem 'pry', :group => [:test, :development]
gem 'guard', :group => [:test, :development]
gem 'guard-rspec', :group => [:test, :development]
gem "factory_girl_rails", ">= 3.1.0", :group => [:test, :development]
gem 'rr', :group => [:test, :development]

gem 'devise', '>= 4.0.0'
gem 'rest-client'
# 三方平台 OAuth 验证登陆
gem "omniauth"
gem "omniauth-douban", :git => "git://github.com/ballantyne/omniauth-douban.git"
gem "redis-objects"
